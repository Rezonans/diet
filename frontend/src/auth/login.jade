h2 Login form

form(ng-submit="submit()" name="loginForm")
  .form-group
    input.form-control(name="email" ng-model="user.email" type="email" placeholder="Email" required)
    div.text-danger(ng-if="errors" ng-repeat="error in errors")
      div {{error}}
  .form-group
    input.form-control(name="password" ng-model="user.password" type="password" placeholder="Password" required)
  .checkbox
    label
      input(type="checkbox" ng-model="user.remember_me")
      = ' Remember me'
  .form-group
    button.btn.btn-success(type="submit" ng-disabled="loginForm.$invalid") LogIn
    a.btn.btn-link(href="javascript:;" ui-sref="^.registration") Have no account?

